let str = "Hello";         // string
let num = 100;             // number
let isPassed = true;       // boolean
let marks = [90, 85, 78];  // array (object)
let user = {name: "Jatin"};// object
let x;                     // undefined
let y = null;              // null (but typeof is object)
