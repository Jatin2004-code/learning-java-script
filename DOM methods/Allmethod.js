getElementById("id")         // 1 element by ID
getElementsByClassName("c")  // HTMLCollection (multiple)
getElementsByTagName("li")   // HTMLCollection (multiple)
querySelector(".class")      // Pehla match only
querySelectorAll("li")       // NodeList (loopable with forEach)
